<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous"> 
    <link rel="stylesheet" href="/styles.css">
    <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.7.0.min.js"></script>
    <script type="importmap">
        {
            "imports": {
                "three": "https://unpkg.com/three@0.155.0/build/three.module.js",
                "gltf_loader": "https://cdn.jsdelivr.net/npm/three/examples/jsm/loaders/GLTFLoader.js",
                "orbit_control": "https://cdn.jsdelivr.net/npm/three/examples/jsm/controls/OrbitControls.js"
            }
        }
    </script>
    <title>Muyu demo - Display</title>
    <style>
        body { margin: 0; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand navbar-dark bg-dark">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        Menu
                    </a>
                    <div class="dropdown-menu dropdown-menu-dark">
                        <a class="dropdown-item" href="/display">Display</a>
                        <a class="dropdown-item" href="/gallery">Gallery</a>
                        <a class="dropdown-item" href="#">My Collection</a>
                        <a class="dropdown-item" href="/about">About</a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>  

    <audio loop autoplay src="Background_WebpageAtmosphere.wav" type="audio/wav" id="atmosphere"> </audio> 

    <script>
        document.addEventListener('click', musicPlay);
        function musicPlay() {
            document.getElementById('atmosphere').play();
            document.removeEventListener('click', musicPlay);
        }
    </script>

    <a id="button">
        <img src="/ic_code_black_24dp.svg">
    </a>

    <div id="card-container" class="position-absolute bottom-0 start-0">
        <div id="card" class="card ms-5 mb-5" style="width: 21rem;">
            <div class="card-body">
                <h5 class="card-title">Instructions</h5>
                <ol class="card-text">
                    <li>Press the space bar to switch between observation and play modes.</li>
                    <li>In observation mode, drag your mouse to rotate the muyu, and zoom with the mouse wheel.</li>
                    <li>In play mode, the mallet will move with the mouse and make a sound when hitting the muyu.</li>
                </ol>
            </div>
        </div>
    </div>

    <script>
        const urlParams = new URLSearchParams(window.location.search);
        const modelName = urlParams.get('modelName');

        console.log(modelName);

        document.addEventListener("DOMContentLoaded", function() {
            var button = document.getElementById('button');
            var cardContainer = document.getElementById('card-container');
            var card = document.getElementById('card');
            
            card.style.display = 'none';
            button.style.display = 'block';

            $( "#button" ).on( "click", function() {
                button.style.display = 'none';
                $( "#card" ).slideToggle( "slow", function() {                    
                });
            });

            $( "#card" ).on( "click", function() {
                $( "#card" ).slideToggle( "slow", function() {
                    button.style.display = 'block';
                });
            });

        //     button.addEventListener('click', function() {
        //         button.style.display = 'none';
        //         card.classList.toggle('card-visible');
        // if (card.classList.contains('card-visible')) {
        //     card.style.display = 'block'; // Show the card when it's visible
        // } else {
        //     card.style.display = 'none'; // Hide the card when it's not visible
        // }
        //     });

        //     card.addEventListener('click', function() {
        //         card.classList.toggle('card-visible');
        // if (card.classList.contains('card-visible')) {
        //     card.style.display = 'block'; // Show the card when it's visible
        // } else {
        //     card.style.display = 'none'; // Hide the card when it's not visible
        // }
        //         button.style.display = 'block';
        //     });

        });
    </script>

    <script type="module" src="/ObjectControls.js"></script>
    <script id="loader" type="module" src="/main.js" model-name="<%= modelName %>"></script>
</body>
</html>